; main = {
;   ch = make-cenv file: "c.h"
;   ch.glfwInit()
;   ch.glfwTerminate();
; }

(def "main"
  (fn
    () ; args
    ( ; begin body
      (
        (fn (tuple "ch")
          (
            (fn (tuple _) ((ch "glfwTerminate") ()))
            ((ch "glfwInit") ())
          )
        )
        ((env "make-cenv") "c.h")
      )
    ) ; body
  )
)
