; main = {
;   ch = make-cenv file: "c.h"
;   ch.glfwInit()
;   ch.glfwTerminate();
; }

(let "main"
  (fn
    nil ; args
    nil ; result
    ( ; begin body
      (let "ch" ((@env "make-cenv") "c.h")
        (seq
          ((@ch "glfwInit") nil)
          ((@ch "glfwTerminate") nil)
        )
      )
    ) ; body
  )
)
